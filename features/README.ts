/**
 * Feature Module Barrel Exports
 * Each feature folder (rates, booking, orders, home, content) follows this pattern:
 *
 * features/
 *   [feature]/
 *     components/   — React components specific to this feature
 *     hooks/        — Custom hooks for data fetching and state
 *     services/     — Supabase query functions
 *     store/        — Zustand stores (if needed)
 *     types/        — TypeScript type definitions
 *
 * This file serves as documentation. Actual exports are done per-file.
 */

export {};
